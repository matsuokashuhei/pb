{
    "name": "Rust",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".",
        "target": "development"
    },
    "customizations": {
        "vscode": {
            "settings": {
                "terminal.integrated.shell.linux": "/bin/bash"
            },
            "extensions": [
                "rust-lang.rust-analyzer"
            ]
        }
    },
    "features": {
    }
}
